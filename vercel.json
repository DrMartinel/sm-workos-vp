{
  "buildCommand": "pnpm run build",
  "installCommand": "if [ \"$ENVIRONMENT\" = \"hrm\" ]; then pnpm run hrm-install && cd /vercel/path0/app/shared-ui && git pull origin hrm && pnpm install; else pnpm install; fi",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
} 
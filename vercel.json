{
  "buildCommand": "pnpm run build",
  "installCommand": "if [ \"$ENVIRONMENT\" = \"hrm\" ]; then pnpm run vercel-install && pnpm install && cd /vercel/path0/app/shared-ui && git pull origin hrm; else pnpm vercel-install && pnpm install; fi",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
} 